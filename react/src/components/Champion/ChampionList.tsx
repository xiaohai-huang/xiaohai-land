import { ChampionData } from "src/api/hok";

type ChampionListProps = {
  champions: ChampionData[];
};
function ChampionList({ champions }: ChampionListProps) {
  return <view>ChampionList</view>;
}

export default ChampionList;
